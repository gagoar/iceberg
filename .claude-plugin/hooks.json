{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .iceberg/last-score.txt ] && head -c 2000 .iceberg/last-score.txt | jq -Rs '{systemMessage: (\"ICEBERG: \" + (gsub(\"\\n\"; \"\") | .[0:2000]))}' || true"
          }
        ]
      }
    ]
  }
}
