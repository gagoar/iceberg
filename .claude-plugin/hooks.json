{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .iceberg/last-score.txt ] && head -c 2000 .iceberg/last-score.txt | tr -d '\\n' | jq -Rs '{systemMessage: (\"ICEBERG: \" + .)}' || true"
          }
        ]
      }
    ]
  }
}
